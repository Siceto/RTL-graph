interface:
  display_name: "RTL Graph"
  short_description: "Show RTL data flow through necessary modules"
  default_prompt: "Use $rtl-graph to show data flowing through every necessary child module, label boxes only with module type names, and omit all control signals."
