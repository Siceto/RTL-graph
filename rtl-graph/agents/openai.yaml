interface:
  display_name: "RTL Graph"
  short_description: "Generate clean RTL architecture diagrams without Yosys"
  default_prompt: "Use $rtl-graph to generate and visually validate a clean RTL block diagram."
