interface:
  display_name: "RTL Graph"
  short_description: "Draw flat RTL module arrays and data rails"
  default_prompt: "Use $rtl-graph to preserve repeated RTL instances, align module stages, and show shared data buses as clean rails without control clutter."
